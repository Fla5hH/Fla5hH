# SSRF

### 1、什么是CSRF？

CSRF(Cross-Site Request Forgery:跨站请求伪造)  
由于服务端依赖浏览器自动携带Cookie等身份凭证来识别用户请求的合法性，但又没有对请求的来源、关键参数做严格的额外校验（如Token、Referer检查等）  
导致攻击者可以构造恶意请求并诱使已登录用户触发，从而让服务器误以为是用户本人发起的合法操作  
数据流：攻击者---->用户浏览器（自动携带用户凭证）---->服务器
